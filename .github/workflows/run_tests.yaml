name: NuMojo Unit Tests
on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  testing-numojo:
    name: with ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["macos-14"]

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install pixi
        run: |
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "$HOME/.pixi/bin" >> $GITHUB_PATH
          export PATH="$HOME/.pixi/bin:$PATH"

      - name: Pixi install
        run: |
          pixi install --locked

      - name: Build package
        run: |
          pixi run mojo precompile numojo -o numojo.mojoc
          cp numojo.mojoc tests/

      - name: Run Core Tests
        run: |
          echo "Testing core modules..."
          pixi run test_core

      - name: Run Routine Tests
        run: |
          echo "Testing routines..."
          pixi run test_routines

      - name: Cleanup
        if: always()
        run: |
          rm -f tests/numojo.mojoc
